import Home from './home/Home';
import Products from './products/Products';

const routes = [
    {
        path: '/',
        exact: true,
        component: Products
    }
];

export default routes;
